import { TestBed, inject } from '@angular/core/testing';

import { DashboardproviderserviceService } from './dashboardproviderservice.service';

describe('DashboardproviderserviceService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DashboardproviderserviceService]
    });
  });


  // it('should be created', inject([DashboardproviderserviceService], (service: DashboardproviderserviceService) => {
  //   expect(service).toBeTruthy();
  // }));

});
